#include <stdio.h>

int main(){
    int l,b;
    printf("Enter the length and breath of Rectangle ");
    scanf("%d,%d",&l,&b);
    
    printf("The Area of the Rectangle is %d",l*b);
    return 0;
}
